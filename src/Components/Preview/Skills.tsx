import React from "react";

const Skills = ({
  skills,
  languages,
  tools,
}: {
  skills: {
    skill: string;
    level: string;
    yearsOfExperience: string;
  }[];
  languages: {
    language: string;
    proficiency: string;
  }[];
  tools: {
    tool: string;
    level: number;
  }[];
}) => {
  return <div></div>;
};

export default Skills;
