import React from "react";
import CvData from "../../Types/CvData";

const Edit = ({
  data,
  setData,
}: {
  data: CvData;
  setData: (data: CvData) => void;
}) => {
  return <div></div>;
};

export default Edit;
